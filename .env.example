# 应用秘钥（用于 cookie 签名），请使用任意长随机字符串
# 可用命令生成：python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=please-change-me-to-a-long-random-string

# SQLite 数据库文件路径（相对项目根目录）
DB_PATH=data/tradernote.db

# 图片上传目录（相对项目根目录）
UPLOAD_DIR=uploads

# 会话保持天数（cookie 持久化时长）
SESSION_DAYS=30
